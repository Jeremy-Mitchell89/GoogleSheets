const Home = () => {
  return (
    <div>
      <button
        onClick={() => {
          console.log("test");
        }}
      >
        Test
      </button>
    </div>
  );
};

export default Home;
